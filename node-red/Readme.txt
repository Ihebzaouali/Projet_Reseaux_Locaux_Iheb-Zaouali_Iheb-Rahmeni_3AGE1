This project uses MQTT with Mosquitto authentication.

To test:
- Import the Node-RED flows
- Edit MQTT nodes:
  - Broker IP address
  - Username
  - Password
- Deploy

MQTT authentication is required (username/password).